let encoding = ...; // Some value you want to assign

// Object with property shorthand
let obj = {
    encoding, // This assigns the value of `encoding` variable to `obj.encoding`
    otherProperty: value
};
